import React from "react";
import ReactDOM from "react-dom/client";
import { GifWizardApp } from "./GifWizardApp";

ReactDOM.createRoot( document.getElementById( 'root' ) ). render(
    <React.StrictMode>
        <GifWizardApp/>
    </React.StrictMode>
);